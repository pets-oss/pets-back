export default interface AnimalDetails {
    animal_id: number,
    breed: string | null,
    species: string | null,
    gender: string | null,
    color: string | null,
    birth_date: string | null,
    weight: number | null,
    allergy: string | null,
    food: string | null
}
