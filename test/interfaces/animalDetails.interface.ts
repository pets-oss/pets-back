export default interface AnimalDetails {
    animalId: number,
    breed: string | null,
    species: string | null,
    gender: string | null,
    color: string | null,
    birthDate: string | null,
    weight: number | null,
    allergy: string | null,
    food: string | null
}
