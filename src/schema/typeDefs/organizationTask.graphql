type OrganizationTask {
    id: Int!
    title: String
    description: String
    organizationId: Int
    isDone: Boolean
}

type Query {
    organizationTasks: [OrganizationTask]
    organizationTask(id: Int): OrganizationTask
}